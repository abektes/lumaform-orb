{
  "name": "lumaform-orb-monorepo",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run dev -w @lumaform/studio",
    "build": "npm run build -w @lumaform/studio",
    "preview": "npm run preview -w @lumaform/studio",
    "test": "node scripts/run-tests.mjs"
  }
}
