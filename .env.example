# Copy to .env.local to configure a local build. Nothing here is required —
# the studio runs fully without it.

# Google Analytics measurement ID. Leave unset and no analytics script is
# injected and no request is made. Set this only for your own deployment; a fork
# that inherits someone else's ID reports into their property.
# VITE_GA_ID=G-XXXXXXXXXX
